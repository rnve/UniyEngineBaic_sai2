namespace ClassUsagn
{
    // 값타입 vs 참조 타입
    // 값을 복사해서 직접 할당함. (int, float, double, struct ..)
    // 참조 타입 : 해당 함 영역의 참조(주소) 를 할당함. (class, string)

    // 클래스는 참조타입
    class SwordMan
    {
        uint jy; = 1;
        float exp;
        bool jsAvailable;
        char gender;
        string nama3;
    }
}

